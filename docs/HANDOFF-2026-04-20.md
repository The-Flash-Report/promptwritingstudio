# Session handoff — 2026-04-20

Resume a future session (on this machine or another) from section 4.

## 1. Session summary

- Completed a six-task content-pivot sweep on PWS (IA audit #37, hub QA #42, QA quick-wins, course-CTA triage #38, prose rewrite on six pages). Three commits landed on main (`b3923df`, `05e4050`, `2a7e6d3`).
- Built two new reusable slash commands into `~/claude-config/commands/`: `/pws-health` (site drift check) and `/handoff` (this doc generator). Pushed to `bryancollins99/claude-config` (commit `3e0deaa`).
- Built the Claude Code Skills Catalogue hub at `/claude-code-skills` — 45 skills across 10 categories, every entry verified against a permissive-licence source (MIT / Apache-2.0 / BSD / CC0 / Unlicense). Data file is SSOT.
- Hub ships with index page, dynamic `[slug]` route, category drill-down, nav+footer wiring, sitemap, and llms.txt entry.
- `/pws-health` extended to cover the new skills data — licence check, freshness check, cross-link check across all four Claude hubs.

## 2. Current state

- **Branch:** `main`, tracking `origin/main`. Clean history, conventional commits.
- **Uncommitted (all part of the skills hub — intended next commit):**
  - `M components/layout/Footer.js` — added Skills Catalogue link in Claude Code Hub block
  - `M components/layout/Header.js` — added Skills Catalogue entry to Learn dropdown
  - `M pages/sitemap.js` — added human-sitemap entry for /claude-code-skills
  - `M public/llms.txt` — added /claude-code-skills to Core Claude Code Pages section
  - `M public/sitemap.xml` — added /claude-code-skills URL with weekly changefreq, 0.9 priority
  - `?? data/claude-code-skills.json` — new SSOT (45 skills, 10 categories, _meta with sources + excluded list)
  - `?? pages/claude-code-skills/` — three new pages (`index.js`, `[slug].js`, `category/[category].js`)
- **Last deployed commit:** `2a7e6d3` (Netlify auto-deploy from main). Live at promptwritingstudio.com.
- **Deploy target:** Netlify site ID `cb77cd5c-8d91-435f-89b2-d8de25449f8c`. Auto-deploys on push to main. Build command: `npm run build`, publish `.next/`.

## 3. Architecture delta

### New pages / routes
- `pages/claude-code-skills/index.js` — hub landing with filter (search + category + tier), card grid, excluded-repos transparency block, FAQ
- `pages/claude-code-skills/[slug].js` — dynamic per-skill page. Sections: hero with badges, short answer, when to use, setup, example, source+attribution, caveats, related skills
- `pages/claude-code-skills/category/[category].js` — category drill-down, skills grouped by tier (High signal / Solid / Situational)

### New data files / SSOT
- `data/claude-code-skills.json` — 45 skills across 10 categories. `_meta` includes lastVerified, curationPolicy, sources list (8 permissive repos), and excluded list (hesreallyhim/awesome-claude-code — CC BY-NC-ND). Every skill carries: slug, category, tier, tags, purpose, shortAnswer, whenToUse, setup steps, example transcript, source {type, author, authorUrl, repo, repoUrl, licence}, caveats, related[].

### Components / agents / skills (global)
- `~/.claude/commands/pws-health.md` — site drift check (also copied to `~/claude-config/commands/` and pushed)
- `~/.claude/commands/handoff.md` — session handoff generator (also copied to `~/claude-config/commands/` and pushed)
- `components/LastVerified.js` — now accepts `label` prop (default "Pricing verified" preserved). Used across pricing pages, MCP stack, and skills hub.

### Schema / data-shape changes
- `data/claude-models.json`: Haiku 3 moved from `"status": "deprecated"` (retiresOn 2026-04-19) to `"status": "retired"` (retiredOn 2026-04-19) — deprecation date had passed.

### Dependency changes
_None._

## 4. Outstanding work

Ordered by priority.

1. **(P1) Build two meta pages for the Skills hub.** Not yet created, but linked from the hub index:
   - `pages/claude-code-skills/install-guide.js` — "install a skill in 60 seconds" walkthrough. Referenced from hub index footer.
   - `pages/claude-code-skills/build-your-own.js` — SKILL.md template walkthrough. Referenced from hub index footer.
   These are currently **dead links** until built. Build before the hub index ships publicly, or remove the links in `pages/claude-code-skills/index.js` (bottom `<section>` block).
2. **(P1) Commit the Skills hub.** See section 2 for files. Conventional commit message suggestion: `feat: ship Claude Code Skills catalogue hub (/claude-code-skills, 45 skills)`. Then `git push origin main` — Netlify auto-deploys.
3. **(P2) SEO/AEO pass on the hub** — Bryan is running this on another machine. Inputs it needs: `data/claude-code-skills.json`, `pages/claude-code-skills/*.js`. Specifically: target-keyword research (`claude code skills`, `awesome claude code alternatives`, `reusable claude code skills`), LLM-citation check via the `/seo` toolkit's `/llm` command, meta-title optimisation on the dynamic `[slug]` pages if target queries surface per-skill intent.
4. **(P2) Run `/pws-health` after deploy** — it will flag residual HUB-LINK items between the four hubs (now includes `/claude-code-skills`). Patch those cross-links in `pages/claude-code-mcp-stack.js`, `pages/claude-pro-vs-max-vs-api.js`, `pages/calculators/claude-plan-picker.js` to include `/claude-code-skills`.
5. **(P2) Audit `pages/best-ai-tools.js`** — `/pws-health` flagged a "Explore Our Courses" CTA at line 756. Confirm whether this is a legit Tier A course-push (keep) or a Tier B learning page that should swap to `/claude-code-guide`.
6. **(P3) Tighten `/pws-health` regex** — current prose check false-positives on `pages/terms-of-service.js` (legit ToS language) and `components/ui/OrganizationSchema.js` (schema.org Course markup). Add a denylist of files to skip.
7. **(P3) Remove the note about 27 items** in `tasks.md` CLAUDE-PIVOT section — that triage is done and the current count is accurate (18 swapped, 15 kept).
8. **(P3) Populate more skills** beyond 45 — Bryan said "best in class". Targets for next session: 60-80 skills. Categories currently light on entries: `meta` (3), `legal` (2). Research more entries from the 16 licence-cleared repos already identified in content-researcher output (see section 5).

## 5. Context to reload next session

Read these, in order:

- `~/src/promptwritingstudio/CLAUDE.md` — stack, conventions, CTA rules.
- `~/src/promptwritingstudio/.claude/rules/coding-style.md`, `git-workflow.md`, `security.md` — project rules.
- `~/src/promptwritingstudio/tasks.md` — still-open #29 (slash-command registry, **now partially superseded by the skills hub**) and #38 (done, see section 3 above).
- `~/.claude/projects/-Users-bryancollins/memory/feedback_attribution.md` — licence + attribution rules that govern any future skills hub additions.
- `~/.claude/commands/pws-health.md` — the drift check. Run first thing next session.
- `~/.claude/commands/handoff.md` — this doc was generated by following this template.
- `data/claude-code-skills.json` — the SSOT for the hub. Every hub page reads from this.

The content-researcher brief from this session contains 16 licence-cleared repos + 20 individual skills not yet featured in the hub. Worth expanding from that list in future sessions. Original research output lives in the session transcript — search for "Research Brief: Claude Code Skills Catalogue Sources" in the conversation.

## 6. Decisions made (and why)

- **Licence gate: permissive only** → reason: Bryan flagged this explicitly ("don't plagiarise"). Evidence: his feedback memory, plus the critical finding that `hesreallyhim/awesome-claude-code` (the biggest community list) is CC BY-NC-ND and cannot be republished. Impact: the hub's differentiator is licence transparency.
- **45-skill MVP, not 80** → reason: time-boxed for this session. Evidence: 45 skills clear the quality bar with the sources already verified; adding more thinly-verified entries would dilute the "best in class" promise. Future sessions expand from the 16 vetted repos.
- **Three-tier system (signal / solid / situational)** → reason: LLMs cite ranked lists. Evidence: AEO research patterns + Bryan's stated goal of LLM-citability. Tiers give a quote-ready "here's what's high-signal" structure.
- **CTA policy on hub pages: course URL only where user is in buying mode** → reason: Bryan's Claude pivot narrative. Evidence: the #38 triage outcome (18 pages swapped, 15 kept). The skills hub is a learning surface → `/claude-code-guide` CTAs, not course CTAs.
- **`/handoff` skill pushed to `~/claude-config/` rather than only local `~/.claude/`** → reason: Bryan explicitly asked for cross-machine reuse. Evidence: `~/claude-config/` is a git-backed repo with remote origin `bryancollins99/claude-config`. Local-only skills don't survive machine changes.
- **Skipped `/seo` toolkit integration this session** → reason: DataForSEO + Anthropic API keys returned 401 on this machine. Evidence: toolkit path was wrong (`~/src/seo-agent/`, not `~/src/seo-toolkit/`) and `.env` was absent after the fresh pull. Bryan is running SEO/AEO on another machine.

## 7. Known gotchas

- **SEO toolkit path** is `~/src/seo-agent/`, not `~/src/seo-toolkit/`. The `/seo` slash command at `~/.claude/commands/seo.md` hardcodes the wrong path — fix when you regain access to the toolkit's `.env`.
- **Toolkit auth is broken** on this machine: DataForSEO 401 on `/volumes`, Anthropic 401 on `/llm`. Needs `.env` populated with valid keys before any `/seo` subcommands work here.
- **`~/.claude/` and `~/claude-config/` are not auto-synced bidirectionally** — there's periodic `auto:` commits on `~/claude-config/` but new skills in `~/.claude/commands/` don't automatically appear there. Manual mirror required: `cp ~/.claude/commands/<skill>.md ~/claude-config/commands/` then `cd ~/claude-config && git add/commit/push`.
- **Next.js version is 13 Pages Router** (not App Router) — dynamic routes use `getStaticPaths` + `getStaticProps`, not server components. Respected throughout the skills hub build.
- **`~/.claude/settings.local.json` in the PWS repo** has a permissive allowlist put in place this session to reduce permission prompts. Deny list still blocks force-push, hard reset, branch delete, and `rm -rf`. Keep it.

## 8. Next-session kickoff prompt

```
Read docs/HANDOFF-2026-04-20.md. Resume from section 4.

Priorities in order:
1. Build the two missing meta pages (install-guide, build-your-own) — they are linked from the hub index and currently dead.
2. Commit and push the Skills hub (files listed in section 2).
3. After deploy, run /pws-health to surface the HUB-LINK regressions now that a fourth hub exists — patch the three existing hub pages to link to /claude-code-skills.

Don't touch the CTA triage outcomes on the industry pages — that work is shipped. Don't rerun the /seo toolkit on this machine — auth is broken, Bryan is running SEO on another machine.
```
